.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch


# PlatformIO build folder (all compiled objects)
.pio/

# PlatformIO temporary files
.platformio/

# VS Code workspace and settings
.vscode/
!.vscode/settings.json   # Keep settings.json if you want to share project config
!.vscode/tasks.json      # Optional: keep shared tasks
!.vscode/launch.json     # Optional: keep shared launch/debug config

# macOS system files
.DS_Store

# Compiled objects and binaries (if outside .pio)
*.elf
*.bin
*.hex
*.map

# Backup files
*~
*.swp
*.tmp

# Logs
*.log

# Library dependencies managed by PlatformIO
# (optional: comment this if you want to check in library files)
.pio/libdeps/

# IntelliJ / CLion project files (if ever used)
.idea/

# CMake cache files (if any)
CMakeLists.txt.user
CMakeFiles/

# Arduino IDE build files (if ever used)
*.cache/
*.tmp/